% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar-report.R
\name{tar_physio_report}
\alias{tar_physio_report}
\title{Quarto report pipeline (target factory)}
\usage{
tar_physio_report(name = "report", path = NULL)
}
\arguments{
\item{name}{Target name (string).}

\item{path}{Path to the \code{.qmd}. Defaults to the bundled template.}
}
\value{
A single target object (requires the \code{tarchetypes} and \code{quarto}
packages plus the Quarto CLI at build time).
}
\description{
Wraps \code{tarchetypes::tar_quarto_raw()} so a rendered report becomes a tracked
target that re-renders when any embedded result changes. The \code{.qmd} reads
results with \code{targets::tar_read()}; targets discovers those dependencies
automatically.
}
\examples{
# list(tar_ecg_hrv(), tar_physio_report())
}

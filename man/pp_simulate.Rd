% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp-data.R
\name{pp_simulate}
\alias{pp_simulate}
\title{Simulate a synthetic PhysioExperiment for demos and tests}
\usage{
pp_simulate(
  modality = c("eeg", "emg", "eda"),
  seed = 1L,
  n_channels = NULL,
  duration = NULL,
  sampling_rate = NULL
)
}
\arguments{
\item{modality}{One of "eeg", "emg", "eda".}

\item{seed}{Integer RNG seed (fixed so pipelines stay reproducible).}

\item{n_channels, duration, sampling_rate}{Optional geometry overrides.}
}
\value{
A \code{PhysioExperiment} (time x channels) with a \code{raw} assay.
}
\description{
Deterministic (seeded) synthetic signals per modality. Clearly synthetic —
for exercising a pipeline's mechanics, not for making claims.
}
\examples{
if (requireNamespace("PhysioCore", quietly = TRUE)) {
  pe <- pp_simulate("eeg")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar-ecg-hrv.R
\name{tar_ecg_hrv}
\alias{tar_ecg_hrv}
\title{ECG heart-rate-variability pipeline (target factory)}
\usage{
tar_ecg_hrv(
  name = "ecg",
  records = NULL,
  channel = 1L,
  detector = "pan_tompkins"
)
}
\arguments{
\item{name}{Target-name prefix (a string; targets are \code{"<name>_pe"} etc.).}

\item{records}{\code{NULL} to use the bundled demo record (MIT-BIH 100 excerpt),
or a character vector of WFDB record bases (paths without extension).}

\item{channel}{Lead index to analyse (MIT-BIH 100: 1 = MLII, 2 = V5).}

\item{detector}{\code{ecgDetectRpeaks()} method (e.g. "pan_tompkins", "hamilton").}
}
\value{
A list of target objects to splice into a \verb{_targets.R} pipeline.
}
\description{
Builds a standard resting-ECG pipeline as a list of \code{targets}:
\verb{read -> lead -> R-peaks -> RR -> \{HRV time, HRV freq\} -> summary}, plus an
RR-tachogram figure. With more than one record it fans out via dynamic
branching (one branch per record) and adds a combined cohort table.
}
\examples{
# In _targets.R:
#   library(targets); library(PhysioPipe)
#   list(tar_ecg_hrv())                       # bundled demo, single record
#   list(tar_ecg_hrv(records = my_bases))     # cohort (dynamic branching)
}
\seealso{
\code{\link[=tar_eeg_bandpower]{tar_eeg_bandpower()}}, \code{\link[=tar_emg_features]{tar_emg_features()}}, \code{\link[=tar_eda_scr]{tar_eda_scr()}}
}
